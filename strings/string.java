package strings;
//strings in java are object of the stringClass

//Strings areimmutable and cannot be changed, operations like concatination create new strings

//methods: length(),charAt(),substring(),indexOf(),equals(),equalsIgnoreCase(),toUpperCase(),toLowerCase()

//.equals() compare components of string, == compares memory referneces
public class string {
    public static void  main(String[]args){
        //declare a  srting
        String str1 = "Hello";//using string lateral
        String str2 = new String("world");//using constructor
    }
}
